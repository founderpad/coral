const Link = {
    Link: {
        // variants: {
        // 	primary: ({ colorScheme = 'fpGrey' }) => ({
        // 		color: `${colorScheme}.500`,
        // 		_hover: {
        // 			color: `${colorScheme}.600`
        // 		}
        // 	})
        // }
        baseStyle: {
            _hover: {
                textDecoration: 'none'
            }
        }
    }
}

export default Link;