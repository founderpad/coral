CREATE TABLE "public"."matchmake_preferences" ("user_id" uuid, "looking_for" text, "skills" text, PRIMARY KEY ("user_id") , FOREIGN KEY ("user_id") REFERENCES "auth"."users"("id") ON UPDATE restrict ON DELETE restrict);
