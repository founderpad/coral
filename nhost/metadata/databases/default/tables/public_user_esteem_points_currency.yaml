table:
  name: user_esteem_points_currency
  schema: public
configuration:
  column_config:
    user_id:
      custom_name: userId
  custom_column_names:
    user_id: userId
  custom_root_fields: {}
select_permissions:
  - role: user
    permission:
      columns:
        - currency
        - points
        - user_id
      filter: {}
